def render_video(timeline: dict, output_path: str = "/tmp/hybrid.mp4") -> str:
    """Render a video timeline to *output_path*.

    This is a placeholder; integrate ffmpeg / moviepy to stitch clips and
    overlays in a real deployment.
    """
    return output_path
